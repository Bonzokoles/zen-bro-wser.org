# build output
dist/
# generated types
.astro/
# test coverage
coverage/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.local
.env.production

# Wrangler local secrets (dev only — NEVER commit)
.dev.vars
.dev.vars.*

# workspace secrets (API keys - never commit)
.workspace_meta/secrets/

# workspace logs and meta (local only)
.workspace_meta/**/*.log
debug.log

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Local workspace meta (submodule-free)
.workspace_meta/awesome-copilot
.workspace_meta/History/
.workspace_meta/staging/
.workspace_meta/MASTER_REPAIR_PLAN.md

old_app/
